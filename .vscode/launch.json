{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Launch backend",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/backend/app.py",
      "cwd": "${workspaceFolder}/backend",
      "console": "integratedTerminal",
      "justMyCode": true,
      "preLaunchTask": "Install backend dependencies"
    }
  ]
}
